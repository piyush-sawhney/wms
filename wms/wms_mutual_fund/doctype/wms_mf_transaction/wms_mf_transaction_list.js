frappe.listview_settings["WMS MF Transaction"] = {
	add_fields: ["folio_number", "request_type", "scheme", "amount", "units"],
    hide_name_column: true, // hide the last column which shows the `name`
    hide_name_filter: true,
};
